function values = estimator_NW(probes1, probes2)
x1 = min(probes1);
x2 = max(probes1);
size = length(probes1);
h = 0.2;
x = linspace(x1,x2,size);

for i=1:size
    sum = 0;
    for j=1:size
        sum = sum + core_keys(prob)
    end
end


end